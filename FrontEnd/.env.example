# 백엔드 주소. Cloudflare Pages 환경변수에도 동일하게 등록한다.
# 로컬 개발에서는 비워두면 vite 프록시(/api → 127.0.0.1:8000)를 탄다.
VITE_API_BASE_URL=https://api.example.com

# 개발용 프록시 대상 (선택)
VITE_DEV_API_TARGET=http://127.0.0.1:8000
